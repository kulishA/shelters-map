version: '3'
services:
  database:
    image: postgres:latest
    ports:
      - '5432:5432'
    env_file:
      - .env
    volumes:
      - ./database/data:/var/lib/postgresql/data/
#  redis:
#    image: redis:6.0-alpine
#    ports:
#      - '6379:6379'